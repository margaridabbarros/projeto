---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { TRANSITION_BEFORE_PREPARATION } from 'astro:transitions/client';
const allPosts = await Astro.glob('../pages/posts/*.md');
const pageTitle = "Novo My Astro Learning Blog";
---
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width" />
    <title>{pageTitle}</title>
  </head>
  <body>
    <Header />

    <h1>Portfolio</h1>
    <p>This is where I'll write about my journey and projects.</p>
    <!--
    <ul>
      {allPosts.map((post) => <li><a href={post.url}>{post.frontmatter.title}</a></li>)}
    </ul>
    -->

    <div class="card">
      <img src="/img/Margarida_Barros.png" alt="" class="profile">

      <h2>Instagram New Feature</h2>
      <p>This is a text to describe this project in my portfolio. This might be truncated but I'm still not sure about that. Let's see how it goes.</p>

    </div>

    <Footer />
  </body>
</html>
